apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: monitoring
spec:
  interval: 5m
  chart:
    spec:
      chart : podinfo
      sourceRef:
        kind: HelmRepository
        name: podinfo
        namespace: podinfo
